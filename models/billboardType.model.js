import BaseModel from './base.model.js';

class BillboardTypeModel extends BaseModel {
    constructor() {
        super('billboard_type', 'billboard_type_id');
    }
    // Mở rộng thêm code ở đây
}

export default BillboardTypeModel;
