import BaseModel from './base.model.js';

class ImageModel extends BaseModel {
    constructor() {
        super('image', 'image_id');
    }
    // Mở rộng thêm code ở đây

}

export default ImageModel;
